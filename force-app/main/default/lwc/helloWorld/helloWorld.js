import { LightningElement } from 'lwc';
export default class HelloWorld   extends LightningElement {
  // Uncomment the following method to see the console log on component load
  // This is useful for debugging and confirming that the component is loaded correctly
  //
  // Note: This method will run every time the component is inserted into the DOM,
  // which includes page reloads or navigation to a different page where this component is used.
  //
//   connectedCallback() {
//     // Visible proof in DevTools console:
//     // Each reload pe current time print hoga
//     // so you know fresh code load hua
//     // (F12 press karke Console tab dekho)
//     // Example: "helloWorld loaded @ 13:42:10"
//     // ↓↓↓
//     // NOTE: Time zone aapke system ka hoga
//     console.log('helloWorld loaded @ ' + new Date().toLocaleTimeString());
//   }
// }    
}
